[codespell]
skip = ./target,./.git
# Boolean flags: use `key =` with nothing after `=` (not `= true` — that breaks argparse).
check-filenames =
check-hidden =
# renderD: serde/JSON field name; mape: MAPE (mean absolute percentage error) acronym
ignore-words-list = renderD,mape
